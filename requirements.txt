fastapi>=0.95.0          # Utilisé dans app.py, auth.py (FastAPI, Request, Reponses, etc.)
sqlmodel>=0.0.8          # Utilisé dans app.py, models.py (Session, SQLModel, Field, select)
SQLAlchemy>=2.0.0        # Utilisé dans database.py, models.py (Column, ForeignKey, Integer, String)
uvicorn>=0.22.0          # Utilisé dans app.py (serveur ASGI pour lancer l'application)
pydantic>=1.10.0         # Utilisé dans app.py (BaseModel pour la validation des données)
starlette>=0.27.0        # Utilisé dans app.py (SessionMiddleware pour la gestion des sessions)
python-dotenv>=1.0.0     # Utilisé dans app.py, auth.py (load_dotenv pour charger les variables d'environnement)
requests>=2.31.0         # Utilisé dans auth.py (appels à l'API Microsoft Graph)
msal>=1.22.0             # Utilisé dans auth.py (Authentification Microsoft Entra ID)
openpyxl>=3.1.2          # Utilisé dans app.py (load_workbook pour l'import d'étudiants via Excel)
python-multipart>=0.0.6  # Requis par FastAPI dans app.py (pour Form et UploadFile)
Jinja2>=3.1.2            # Requis par FastAPI dans app.py (pour Jinja2Templates)
itsdangerous>=2.1.2      # Requis par Starlette dans app.py (pour SessionMiddleware)
pandas>=2.0.0            # Utilisé pour l'agrégation des données et l'export CSV
requests-cache           # Utilisé pour mettre en cache les requêtes LLM
markdown-it-py           # Utilisé pour transformer le markdown du LLM en HTML
